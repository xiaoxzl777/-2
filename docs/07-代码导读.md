# 七、后端代码导读

> 写给自己看的：每个文件夹是干什么的、为什么需要、哪些要吃透、哪些当黑盒就行。
> 后端约 5700 行 / 50 多个文件。需要读懂的只有约 700 行（见第五节的阅读顺序）。

## 1. 先建立对应关系：它就是一个分层的 Web 后端

和 Spring Boot 的分层是一一对应的：

| 这个项目 | Spring Boot 里叫 | 干什么 |
|---|---|---|
| `app/api/` | Controller | 接请求、校验参数、返回 JSON。每个文件 40–110 行，很薄 |
| `app/services/` | Service | 编排流程、读写数据库、开后台任务 |
| `app/models.py` | Entity | 11 张表 |
| `app/schemas.py` | DTO / VO | 请求体、响应体长什么样（Pydantic） |
| `app/deps.py` | 拦截器 + 参数解析器 | "当前登录用户是谁""这份简历是不是你的""解析完了没有" |
| `app/config.py`、`app/database.py` | application.yml、DataSource | 读 `.env`、数据库连接 |
| `app/errors.py`、`app/security.py` | 全局异常处理、JWT 工具类 | 统一响应 `{code, message, data}`、密码哈希与令牌 |
| `app/main.py` | 启动类 | 启动时检查 MySQL / Redis、清理上次没跑完的任务、挂路由 |

**以上是普通 Web 后端，扫一眼即可。** 真正新的东西在下面五个包里。它们有一个共同约定：
**只做计算，不碰数据库**（输入是普通数据，输出也是普通数据），算完交给 Service 去存。好处是能单独测试、单独做实验。

| 包 | 行数 | 一句话 |
|---|---|---|
| `app/parser/` | 1250 | PDF → 一段全文 + 每块文字的位置 + 结构化信息 |
| `app/diagnose/` | 520 | 找出简历写得不好的地方（7 条规则 + 大模型） |
| `app/matching/` | 550 | 把 JD 拆成要求，逐条判断简历满不满足 |
| `app/graphs/` | 420 | 用 LangGraph 把上面的步骤编排成流程图（并行、分支） |
| `app/llm/` + `app/retrieval/` + `app/cache/` | 780 | 调大模型、调向量模型、查向量库、用 Redis |

## 2. 整个系统只有三个核心想法

所有看起来复杂的代码，都是这三件事的不同写法。答辩和面试讲清这三条就够了。

### ① 坐标系

解析完简历后拼出一个 `full_text`（所有文字按阅读顺序用换行连起来），**之后永远不改**。
系统里任何东西——一个块、一条经历、一个问题的证据、一条匹配依据——都用 `char_start / char_end` 表示
"它是 full_text 的第几个字到第几个字"。

- 前端高亮只需要按区间涂色；
- 代码和测试里到处是 `full_text[start:end] == 这段文字`，检查的就是坐标没有错位；
- 定义坐标系的只有一个函数：`parser/layout.py` 里的 `assign_offsets`。

### ② 模型说的话，必须能在原文里找到（反幻觉）

模型说"这句话有问题"，就拿它引用的那句话去原文里找——先精确找，找不到再模糊找（相似度 ≥ 90）。
**找不到 = 它编的，丢掉并计数。** 这个函数是 `diagnose/evidence.py` 的 `locate_span`，三个地方共用：

| 哪里 | 模型引用的是 | 找不到怎么办 |
|---|---|---|
| 诊断 | 简历里有问题的那句话 | 带着原因让模型重答，最多 3 次；还不行就丢弃 |
| JD 解析 | JD 里提出这条要求的原话 | 这条要求丢弃 |
| 匹配（全文模式） | 简历里能证明满足要求的那句话 | 这条按"未满足"处理 |

被丢弃的数量都落库了（`hallucination_count`），论文里的"幻觉拦截率"就是从这里算的。

### ③ 确定的用规则，模糊的交给模型

| 用代码判（免费、结果稳定） | 问模型 |
|---|---|
| 这条描述里有没有数字 | 这句话是不是空话、有没有夸大 |
| 学历够不够、工作年限够不够 | "有高并发项目经验"算不算满足 |
| 要求的技能词在简历经历里出现过没有 | 简历写的是 MyBatis-Plus，JD 要 MyBatis，算不算 |

规则先跑，规则判不了的才花钱问模型。分数一律用公式算，**不让模型打分**（模型打分不稳定，也说不清为什么是这个分）。

## 3. 一次"投递"从头到尾经过哪些文件

用户点"投递"（`POST /apply`，带上简历 id 和岗位 id）之后：

1. `api/apply.py` 校验：简历是你的吗、岗位看得见吗 → 建两条"待处理"记录（一条诊断、一条匹配）→ 把活丢给后台任务，立刻返回。
2. `services/apply_service.py` 的 `run_apply`（后台）：如果简历还在解析就等它 → 准备好数据 → 跑图 A → 每完成一步往 Redis 发一条进度。
3. `graphs/apply_graph.py`（图 A）：**诊断和匹配同时跑**，两条都完成后判断总分过不过 60。
   - 诊断那条线 `graphs/diagnose_graph.py`：7 条规则扫一遍 → 每条经历**并行**发给模型审 → 去重合并 → 算分。
   - 匹配那条线 `graphs/match_graph.py`：规则先判 → 剩下的要求**并行**走"检索 → 模型判定" → 判成没满足的，用简历全文再问一次（防止检索漏掉）→ 算分。
4. 回到 `apply_service`：调 `diagnose_service.save_result` 和 `match_service.save_result` 把结果写进数据库。
5. 前端通过 `api/task.py`（SSE）实时看到进度；结束后请求 `GET /apply/{id}`，拿到"过没过 + 哪里不符合"。

LangGraph 在这里的价值就两个字：**并行**（7 条经历同时审、15 条要求同时判、诊断与匹配同时跑）。除此之外它只是把步骤连起来。

## 4. 各板块说明

### parser/ —— 解析（按执行顺序）

| 文件 | 干什么 | 要不要细读 |
|---|---|---|
| `extract.py` | 用 PyMuPDF 读出每一行的文字、坐标、字号、是否加粗；扫描件 / 加密文件在这里被拦下 | 扫一眼 |
| `layout.py` | **全项目最难的文件。** 解决"两栏简历从上往下读会左右串行"：找页面中间贯穿上下的空白带，有就分左右栏分别读；再把折行的句子合并成"块"；最后 `assign_offsets` 定下坐标系 | 懂思路即可，不必逐行 |
| `section.py` | 认章节标题（"项目经历""专业技能"）：标题词典 + 字号 / 加粗 / 是否独占一行 打分 | 扫一眼 |
| `pii.py` | 姓名、电话、邮箱在本地用正则抽出来；发给模型之前把它们换成**等长**的 `X`（等长是为了不破坏坐标系） | 读，很短 |
| `structure.py` | 让模型把"项目经历"拆成一条条。关键设计：**模型只回块编号，不回文字**，文字由代码按编号去原文切——模型没有机会改写简历内容 | 读 `_extract_section` 这一个函数 |
| `normalize.py` | 把"2023.9 – 至今"这类日期统一成 `2023-09` | 不用读 |

### diagnose/ —— 诊断

| 文件 | 干什么 |
|---|---|
| `rules.py` | 7 条规则，每条一个小函数：成果没数字、只说做了什么没说结果、弱动词开头、技能列了却没在经历里用到、时间线有空窗或重叠、不利于机器解析、篇幅异常 |
| `llm_review.py` | 把每条经历单独发给模型审；引用在原文里找不到 → 带着原因让它重答，最多 3 次 |
| `evidence.py` | 核心想法②：`locate_span` |
| `scorer.py` | 五个维度各从 100 分往下扣（严重 25 / 中等 12 / 轻微 5），再加权 |
| `types.py` | 两个数据类：`ReviewUnit`（一条送审的经历）、`Finding`（一个被发现的问题） |

### matching/ —— 匹配

| 文件 | 干什么 |
|---|---|
| `jd_parser.py` | 调一次模型把 JD 拆成要求项（必须 / 加分 / 软素质 × 技能 / 学历 / 经验 / 其他），每条必须带 JD 原话 |
| `skill_dict.py` | 155 个技能的同义词词典：认出 "SpringBoot" 和 "Spring Boot" 是同一个；解析简历时顺手把全文里出现的技能都标出来（`skill_mentions`） |
| `matcher.py` | 规则能判的先判（技能词、学历、年限）+ 加权算总分。规则只判十拿九稳的：要求就是技能名本身、且经历里确实用过；带限定语的（"熟悉 Redis 缓存穿透…"）交给模型 |
| `units.py` | 把简历切成一个个可检索的小段：每条经历描述、每段经历的头部（技术栈那行）、教育经历、技能章节每一行、奖项 |
| `llm_judge.py` | 规则判不了的交给模型。两种问法：只给它看检索出的 3 段（RAG），或给它看全文；都要求它指出依据 |

匹配有四种模式，是给论文的对比实验用的，线上只用 `hybrid`：

| mode | 做法 | 用途 |
|---|---|---|
| `dict_only` | 只用规则，不调模型 | 基线 |
| `llm_fulltext` | 不用规则也不检索，全文 + 全部要求一次问模型 | 对照：不用 RAG |
| `llm_rag` | 不用规则，逐条 检索 → 判定 | 对照：只用 RAG |
| `hybrid` | 规则 → RAG 判定 → 没满足的用全文复核 | 线上默认 |

### retrieval/ 与 llm/ —— RAG 和模型调用

| 文件 | 干什么 |
|---|---|
| `retrieval/unit_store.py` | **标准的两阶段 RAG。** 简历小段向量化后存进 Chroma；来一条 JD 要求：先用向量相似度**召回** 10 段，再用 reranker **精排**出 3 段，交给模型判断 |
| `retrieval/chroma_client.py` | 打开嵌入式向量库（数据在 `data/chroma`，不需要单独起服务） |
| `llm/client.py` | 全项目调 DeepSeek **只走这一个函数**，固定五步：查缓存 → 限流 → 调用 → 记账（写 `llm_calls` 表）→ 写缓存。模型输出不是合法 JSON 时不抛异常，而是把错误原因返回，由调用方带着原因重试 |
| `llm/embedding.py` | 调硅基流动的向量接口（bge-m3）和重排接口（bge-reranker） |
| `llm/prompts.py` | 所有提示词集中在这里，每组带版本号；改了提示词就升版本，旧缓存自动失效 |
| `llm/registry.py`、`llm/audit.py` | 模型名 → 模型对象与单价；写审计表 |

### graphs/ —— LangGraph

三张图，见第 3 节。`state.py` 定义图里流动的数据长什么样；带 `Annotated[..., add]` 的字段表示
"多个并行分支会同时往里写，用加法合并"——这是 LangGraph 并行的固定写法。

### cache/ —— Redis 的三个用途，一个文件一个

`llm_cache.py` 模型结果缓存（同样的输入不重复付费）、`ratelimit.py` 限流（每分钟最多多少次调用）、
`pubsub.py` 后台任务的进度推送。`redis_client.py` 是连接。

### services/ 与 api/

一个业务一对文件（`resume` / `job` / `apply` + 诊断、匹配的落库与查询）。后台任务只有两种：
**解析**（上传时触发）和**投递**（诊断 + 匹配 + 初筛）。诊断与匹配不单独触发，只提供查询接口。

### tests/、scripts/、sql/

- `tests/`（约 3000 行）：**不用读。** 它的作用是保证改代码时不会把以前的功能改坏；里面的模型、向量库、Redis 全是假的，不联网。
- `sql/schema.sql`、`sql/seed.sql`：建表与技能词典，手动在 MySQL 里执行。它们由 `scripts/dump_schema.py` / `dump_seed.py` 从 `models.py` 和 `data/skills_seed.csv` 生成，改表后重新生成即可（没有线上数据，不需要迁移脚本）。

## 5. 建议的阅读顺序（约 700 行）

读懂这 6 个文件，答辩和面试会被问到的部分就基本覆盖了：

| 顺序 | 文件 | 行数 | 读完应该能回答 |
|---|---|---|---|
| 1 | `diagnose/evidence.py` | 80 | 怎么判断模型是不是在瞎编？模糊匹配为什么要有？ |
| 2 | `diagnose/rules.py` | 230 | 7 条规则各查什么？证据区间怎么来的？ |
| 3 | `llm/client.py` | 160 | 一次模型调用经过哪五步？缓存的 key 是什么？JSON 解析失败怎么办？ |
| 4 | `graphs/diagnose_graph.py` | 130 | 图有哪些节点？并行是怎么写的（`Send`）？三种 mode 差在哪？ |
| 5 | `retrieval/unit_store.py` | 90 | 两阶段检索是什么？为什么要精排？索引什么时候重建？ |
| 6 | `graphs/match_graph.py` | 120 | 四种 mode 的路径？为什么 RAG 判了"没满足"还要用全文复核？ |
